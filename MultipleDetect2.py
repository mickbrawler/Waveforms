# Idea: Padd values of match list that are less than threshold with 0, "window" slides across first match list, 
# finds peak (+, 	0,-), checks if peak exists in other detectors' match lists at that index, if so store 
# index and value as ?, move on to next peak.

def multidetect():

    
